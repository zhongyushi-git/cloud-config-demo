server:
  port: 3344

spring:
  application:
    name: cloud-config-server
  cloud:
    config:
      server:
        git:
          #github仓库上面的git仓库名字
          uri: https://github.com/zhongyushi-git/spring-cloud-server-config.git